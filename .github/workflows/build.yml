name: Build FAP

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: flipperdevices/flipperzero-ufbt-action@v0.1.2
        with:
          target: fap
